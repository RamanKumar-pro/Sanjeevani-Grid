# Frontend — Sanjeevani Grid

This folder contains the React frontend for Sanjeevani Grid.

Quick start

```bash
cd frontend
npm install
npm run dev
# or: npm start
```

Notes
- The dev server typically runs on `http://localhost:3000`.
- The frontend expects the backend API at the URL configured in the environment (check `src/services` or `.env` files).
